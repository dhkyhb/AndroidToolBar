<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--定义一个declare-styleable，在获取属性的时候为我们自动提供了一个属性数组-->
    <declare-styleable name="TitleBar">
        <!--单独定义一个attr就会在R文件里面生成一个Id,即没有styleable-->
        <attr name="titleText" format="string"></attr>
        <attr name="titleTextSize" format="dimension"></attr>
        <attr name="titleTextColor" format="color"></attr>
        <attr name="titleBackguround" format="reference|color"></attr>

        <attr name="leftText" format="string"></attr>
        <attr name="leftTextSize" format="dimension"></attr>
        <attr name="leftTextColor" format="color"></attr>
        <attr name="leftBackground" format="reference|color"></attr>

        <attr name="rightText" format="string"></attr>
        <attr name="rightTextSize" format="dimension"></attr>
        <attr name="rightTextColor" format="color"></attr>
        <attr name="rightBackground" format="reference|color"></attr>

    </declare-styleable>

    <declare-styleable name="PicAndTextBtn">

        <attr name="text" format="string"></attr>
        <attr name="textSize" format="dimension"></attr>
        <attr name="textColor" format="color"></attr>
        <attr name="textBackground" format="reference|color"></attr>

        <attr name="pic" format="reference|color|integer"></attr>
        <attr name="picBackground" format="reference|color"></attr>

    </declare-styleable>

</resources>